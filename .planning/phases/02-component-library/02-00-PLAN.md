---
phase: 02-component-library
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/mobile/package.json
  - apps/mobile/jest.config.js
  - apps/mobile/tests/setup.ts
  - apps/mobile/tests/components/ui/Button.test.tsx
  - apps/mobile/tests/components/ui/Card.test.tsx
  - apps/mobile/tests/components/ui/Typography.test.tsx
  - apps/mobile/tests/components/ui/Badge.test.tsx
  - apps/mobile/tests/components/forms/TextField.test.tsx
  - apps/mobile/tests/components/forms/Toggle.test.tsx
  - apps/mobile/tests/screens/components.test.tsx
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-09, COMP-12, COMP-17]

must_haves:
  truths:
    - "jest-expo and @testing-library/react-native are installed as devDependencies"
    - "jest.config.js exists with jest-expo preset and correct moduleFileExtensions"
    - "npx jest --passWithNoTests exits 0 with no failures"
    - "Stub test files exist for all VALIDATION.md test map entries"
  artifacts:
    - path: "apps/mobile/jest.config.js"
      provides: "Jest configuration with jest-expo preset"
      min_lines: 5
    - path: "apps/mobile/tests/setup.ts"
      provides: "Shared test setup with mocks for Reanimated, expo-linear-gradient, @expo/vector-icons"
      min_lines: 10
    - path: "apps/mobile/tests/components/ui/Button.test.tsx"
      provides: "Stub test for Button component"
      min_lines: 5
    - path: "apps/mobile/tests/components/ui/Card.test.tsx"
      provides: "Stub test for Card component"
      min_lines: 5
    - path: "apps/mobile/tests/components/ui/Typography.test.tsx"
      provides: "Stub test for Typography component"
      min_lines: 5
    - path: "apps/mobile/tests/components/ui/Badge.test.tsx"
      provides: "Stub test for Badge component"
      min_lines: 5
    - path: "apps/mobile/tests/components/forms/TextField.test.tsx"
      provides: "Stub test for TextField component"
      min_lines: 5
    - path: "apps/mobile/tests/components/forms/Toggle.test.tsx"
      provides: "Stub test for Toggle component"
      min_lines: 5
    - path: "apps/mobile/tests/screens/components.test.tsx"
      provides: "Smoke test stub for playground screen"
      min_lines: 5
  key_links:
    - from: "apps/mobile/jest.config.js"
      to: "jest-expo"
      via: "preset configuration"
      pattern: "preset.*jest-expo"
    - from: "apps/mobile/tests/setup.ts"
      to: "react-native-reanimated"
      via: "mock setup for Reanimated in test env"
      pattern: "reanimated|mock"
---

<objective>
Install test infrastructure (jest-expo, @testing-library/react-native) and create Jest configuration, shared test setup with required mocks, and stub test files for all components listed in the VALIDATION.md test map.

Purpose: Wave 0 establishes the test foundation so every subsequent plan can run `npx jest --passWithNoTests` as part of its feedback loop. Without this, all plans rely solely on TypeScript compilation for verification, violating the Nyquist sampling requirement for behavior-level testing.

Output: Working Jest configuration, test setup with mocks, and 7 stub test files ready to be filled in as components are built.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library/02-CONTEXT.md
@.planning/phases/02-component-library/02-VALIDATION.md

@apps/mobile/package.json
@apps/mobile/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test dependencies and create Jest configuration</name>
  <files>
    apps/mobile/package.json,
    apps/mobile/jest.config.js,
    apps/mobile/tests/setup.ts
  </files>
  <action>
    **Step 1: Install test dependencies**
    ```bash
    cd apps/mobile && npm install --save-dev jest-expo @testing-library/react-native @testing-library/jest-native @types/jest
    ```

    **Step 2: Create jest.config.js** at `apps/mobile/jest.config.js`:
    ```javascript
    module.exports = {
      preset: 'jest-expo',
      setupFilesAfterFramework: ['./tests/setup.ts'],
      moduleFileExtensions: ['ts', 'tsx', 'js', 'jsx', 'json'],
      transformIgnorePatterns: [
        'node_modules/(?!((jest-)?react-native|@react-native(-community)?)|expo(nent)?|@expo(nent)?/.*|@expo-google-fonts/.*|react-navigation|@react-navigation/.*|@sentry/react-native|native-base|react-native-svg|nativewind|react-native-reanimated|react-native-css-interop)',
      ],
      testMatch: ['**/tests/**/*.test.{ts,tsx}'],
    };
    ```

    **Step 3: Create tests/setup.ts** at `apps/mobile/tests/setup.ts`:
    ```typescript
    // Mock react-native-reanimated
    jest.mock('react-native-reanimated', () => {
      const Reanimated = require('react-native-reanimated/mock');
      Reanimated.default.call = () => {};
      return Reanimated;
    });

    // Mock expo-linear-gradient
    jest.mock('expo-linear-gradient', () => {
      const { View } = require('react-native');
      return {
        LinearGradient: View,
      };
    });

    // Mock @expo/vector-icons
    jest.mock('@expo/vector-icons', () => {
      const { Text } = require('react-native');
      return {
        MaterialIcons: Text,
      };
    });

    // Silence the Animated warning in tests
    jest.mock('react-native/Libraries/Animated/NativeAnimatedHelper');
    ```

    If jest-expo already provides a Reanimated mock via its preset, the manual mock can be conditional or removed. Check jest-expo docs. Either way, having explicit mocks ensures stability.

    **Step 4: Add test script to package.json** (if not already present):
    Add to scripts: `"test": "jest"` in `apps/mobile/package.json`.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>
    - jest-expo and @testing-library/react-native installed as devDependencies
    - jest.config.js exists with jest-expo preset and correct transform patterns
    - tests/setup.ts provides mocks for Reanimated, LinearGradient, MaterialIcons
    - `npx jest --passWithNoTests` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Create stub test files for all VALIDATION.md test map entries</name>
  <files>
    apps/mobile/tests/components/ui/Button.test.tsx,
    apps/mobile/tests/components/ui/Card.test.tsx,
    apps/mobile/tests/components/ui/Typography.test.tsx,
    apps/mobile/tests/components/ui/Badge.test.tsx,
    apps/mobile/tests/components/forms/TextField.test.tsx,
    apps/mobile/tests/components/forms/Toggle.test.tsx,
    apps/mobile/tests/screens/components.test.tsx
  </files>
  <action>
    Create stub test files that describe expected behaviors but skip actual assertions until the components exist. Each stub should:
    1. Import `@testing-library/react-native` render function
    2. Have a `describe` block naming the component
    3. Have `it.todo()` entries describing the key behaviors to test
    4. NOT import the component yet (it does not exist until Plan 01/02/04 creates it)

    **tests/components/ui/Button.test.tsx:**
    ```typescript
    describe('Button', () => {
      it.todo('renders primary variant with gradient');
      it.todo('renders secondary variant with bg-white/5');
      it.todo('renders outline variant with border');
      it.todo('renders disabled state with reduced opacity');
      it.todo('calls onPress when pressed');
      it.todo('does not call onPress when disabled');
      it.todo('renders loading state with ActivityIndicator');
    });
    ```

    **tests/components/ui/Card.test.tsx:**
    ```typescript
    describe('Card', () => {
      it.todo('renders children in body area');
      it.todo('renders header when provided');
      it.todo('renders footer when provided');
      it.todo('calls onPress when pressable card is pressed');
    });
    ```

    **tests/components/ui/Typography.test.tsx:**
    ```typescript
    describe('Typography', () => {
      it.todo('renders body variant by default');
      it.todo('renders h1 variant with heading font');
      it.todo('renders caption variant with smaller text');
      it.todo('applies custom className');
    });
    ```

    **tests/components/ui/Badge.test.tsx:**
    ```typescript
    describe('Badge', () => {
      it.todo('renders success variant with green styling');
      it.todo('renders error variant with red styling');
      it.todo('renders info variant with blue styling');
      it.todo('renders warning variant with yellow styling');
    });
    ```

    **tests/components/forms/TextField.test.tsx:**
    ```typescript
    describe('TextField', () => {
      it.todo('renders with label');
      it.todo('renders placeholder text');
      it.todo('displays validation error from react-hook-form');
      it.todo('renders disabled state');
      it.todo('calls onChange when text is entered');
    });
    ```

    **tests/components/forms/Toggle.test.tsx:**
    ```typescript
    describe('Toggle', () => {
      it.todo('renders in off state by default');
      it.todo('toggles on when pressed');
      it.todo('renders disabled state');
      it.todo('displays label text');
    });
    ```

    **tests/screens/components.test.tsx:**
    ```typescript
    describe('ComponentsScreen', () => {
      it.todo('renders without crashing');
      it.todo('displays Core UI section heading');
      it.todo('displays Form Controls section heading');
      it.todo('displays Feedback section heading');
      it.todo('displays Layout section heading');
    });
    ```

    The `it.todo()` pattern means Jest counts these as pending tests, not failures. They will pass with `--passWithNoTests` and show as "todo" in the output. As each plan creates the actual components, the executor should convert relevant `it.todo()` entries into real tests with assertions.
  </action>
  <verify>
    <automated>cd apps/mobile && npx jest --passWithNoTests 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 7 stub test files exist matching VALIDATION.md test map
    - Directory structure: tests/components/ui/, tests/components/forms/, tests/screens/
    - All stubs use it.todo() for pending behaviors
    - `npx jest --passWithNoTests` exits 0 with todo items listed
  </done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx jest --passWithNoTests` exits 0
- `ls apps/mobile/tests/components/ui/` shows Button.test.tsx, Card.test.tsx, Typography.test.tsx, Badge.test.tsx
- `ls apps/mobile/tests/components/forms/` shows TextField.test.tsx, Toggle.test.tsx
- `ls apps/mobile/tests/screens/` shows components.test.tsx
- `cat apps/mobile/jest.config.js` shows jest-expo preset
</verification>

<success_criteria>
- Jest test runner works with jest-expo preset and all mocks load without error
- 7 stub test files exist with todo items describing expected behaviors
- `npx jest --passWithNoTests` runs in under 15 seconds and exits 0
- Test infrastructure matches VALIDATION.md Wave 0 requirements checklist
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-library/02-00-SUMMARY.md`
</output>
