---
phase: 02-component-library
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - apps/mobile/src/components/index.ts
  - apps/mobile/app/(dashboard)/components.tsx
autonomous: false
requirements: [COMP-17]

must_haves:
  truths:
    - "Master barrel export re-exports all components from ui/, forms/, feedback/, layout/"
    - "Playground displays all components grouped by section: Core UI, Form Controls, Feedback, Layout"
    - "Every component variant is visible without user interaction (no hidden-by-default sections except code snippets)"
    - "Playground is scrollable and readable on both desktop and mobile viewport widths"
    - "Collapsible code snippets show example usage for each component"
  artifacts:
    - path: "apps/mobile/src/components/index.ts"
      provides: "Master barrel re-exporting all component subfolders"
      exports: ["Button", "Card", "Typography", "Badge", "Avatar", "Divider", "Table", "Field", "TextField", "Select", "MultiSelect", "Toggle", "Checkbox", "Modal", "ModalContent", "Alert", "SkeletonLine", "SkeletonCard", "List", "Dropdown", "Tabs", "Accordion", "AccordionItem"]
    - path: "apps/mobile/app/(dashboard)/components.tsx"
      provides: "Complete playground screen with all 4 sections"
      min_lines: 200
  key_links:
    - from: "apps/mobile/src/components/index.ts"
      to: "apps/mobile/src/components/ui/index.ts"
      via: "re-export all ui components"
      pattern: "export.*from.*ui"
    - from: "apps/mobile/src/components/index.ts"
      to: "apps/mobile/src/components/forms/index.ts"
      via: "re-export all form components"
      pattern: "export.*from.*forms"
    - from: "apps/mobile/src/components/index.ts"
      to: "apps/mobile/src/components/feedback/index.ts"
      via: "re-export all feedback components"
      pattern: "export.*from.*feedback"
    - from: "apps/mobile/src/components/index.ts"
      to: "apps/mobile/src/components/layout/index.ts"
      via: "re-export all layout components"
      pattern: "export.*from.*layout"
---

<objective>
Create the master barrel export for the component library and polish the playground screen. Verify all components visually with a human checkpoint.

Purpose: The master barrel export (`src/components/index.ts`) enables clean imports for all consumers (dashboard, about, data fetching, state management screens). The visual checkpoint confirms every component renders correctly with the dark theme, flat borders, and consistent styling before moving to Phase 3.

Output: Master barrel export and visually verified playground screen.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-library/02-CONTEXT.md
@.planning/phases/02-component-library/02-RESEARCH.md
@.planning/phases/02-component-library/02-04-SUMMARY.md

<interfaces>
<!-- All barrel exports from the 4 component subdirectories -->

From apps/mobile/src/components/ui/index.ts:
```typescript
export { default as Button } from './Button';
export { default as Card } from './Card';
export { default as Typography } from './Typography';
export { default as Badge } from './Badge';
export { default as Avatar } from './Avatar';
export { default as Divider } from './Divider';
export { default as Table } from './Table';
```

From apps/mobile/src/components/forms/index.ts:
```typescript
export { default as Field } from './Field';
export { default as TextField } from './TextField';
export { default as Select } from './Select';
export { default as MultiSelect } from './MultiSelect';
export { default as Toggle } from './Toggle';
export { default as Checkbox } from './Checkbox';
```

From apps/mobile/src/components/feedback/index.ts:
```typescript
export { default as Modal } from './Modal';
export { default as ModalContent } from './ModalContent';
export { default as Alert } from './Alert';
export { SkeletonLine, SkeletonCard } from './Skeleton';
```

From apps/mobile/src/components/layout/index.ts:
```typescript
export { default as List } from './List';
export { default as Dropdown } from './Dropdown';
export { default as Tabs } from './Tabs';
export { default as Accordion, AccordionItem } from './Accordion';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create master barrel export and polish playground</name>
  <files>
    apps/mobile/src/components/index.ts,
    apps/mobile/app/(dashboard)/components.tsx
  </files>
  <action>
    **Step 1: Create src/components/index.ts** -- Master barrel that re-exports everything:
    ```typescript
    // Core UI
    export * from './ui';

    // Form Controls
    export * from './forms';

    // Feedback
    export * from './feedback';

    // Layout
    export * from './layout';
    ```

    **Step 2: Polish playground screen** -- Review and refine the complete playground:

    1. **Header area:** Ensure the top of the screen has:
       - `Typography variant="h1"` -- "Component Library"
       - `Typography variant="body" className="text-neutral-400 mt-2 mb-6"` -- Brief description ("Interactive playground showcasing all UI components. Built with NativeWind, Reanimated, and react-hook-form.")

    2. **Section order per user decision:** Core UI -> Form Controls -> Feedback -> Layout
       Each section separated by `Divider` + generous spacing (`mt-10 mb-4`)

    3. **Responsive layout considerations:**
       - ScrollView with `px-4 py-6` on mobile, consider `max-w-4xl mx-auto` wrapper on web for readability
       - Button/Badge rows use `flex-row flex-wrap gap-3` so they wrap on narrow viewports
       - Cards should be full-width on mobile, reasonable max-width on desktop

    4. **Code snippet cleanup:** Ensure all collapsible code snippets are consistent:
       - Toggle button: small `Pressable` with "Show Code" / "Hide Code" text in `text-white/30 text-xs`
       - Code block: `bg-white/5 rounded-xl p-3 mt-2` with monospace text (`font-body text-xs text-neutral-400`)
       - Keep examples concise (3-7 lines showing import + basic usage)

    5. **Bottom padding:** Add `pb-20` at bottom of ScrollView for comfortable scrolling past the last section

    6. **Verify TypeScript:** Run `npx tsc --noEmit` to confirm everything compiles
  </action>
  <verify>
    <automated>cd apps/mobile && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Master barrel at src/components/index.ts re-exports all components
    - Playground section order matches user decision: Core UI, Form Controls, Feedback, Layout
    - Playground has consistent section headings, spacing, and code snippets
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete component library</name>
  <files>apps/mobile/app/(dashboard)/components.tsx</files>
  <action>
    Human visual verification of all components in the playground. Start dev server with `cd apps/mobile && npx expo start --web`, navigate to Components screen, and verify every section.

    **Core UI section:**
    - Typography: All 7 variants with correct fonts (Orbitron uppercase for headings, Inter for body)
    - Button: Primary (blue-cyan gradient), Secondary (bg-white/5), Outline (border-only), Disabled (dimmed), press animation
    - Card: Header/body/footer render, pressable card has scale animation
    - Badge: 4 semantic colors (green/blue/yellow/red) with subtle transparent treatment
    - Avatar: Gradient circle with initials, image variant
    - Divider: Thin horizontal line

    **Form Controls section:**
    - TextField: Shows default, error, disabled states
    - Select: Popover opens below field with options
    - MultiSelect: Chips display, popover with checkboxes + Done
    - Toggle: Animates between on/off
    - Checkbox: Checkmark appears when checked
    - Mini form: Submit with empty fields shows validation errors, submit with valid data shows success

    **Feedback section:**
    - Modal: Opens centered with backdrop, dismisses correctly
    - Alert: 4 semantic types (success/info/warning/error) with icons
    - Dropdown: Popover appears below trigger button

    **Layout section:**
    - Table: Bordered rows, styled header row
    - List: Items with dividers
    - Tabs: Underline indicator switches on tap, content changes
    - Accordion: Sections expand/collapse with smooth animation
    - Skeleton: Pulse animation visible

    **Overall:**
    - Dark theme consistent throughout (no white backgrounds, no shadows)
    - Flat design with borders (no shadows anywhere)
    - Components readable on both narrow (mobile) and wide (desktop) viewports
    - Collapsible code snippets work for each component
    - Section order: Core UI, Form Controls, Feedback, Layout (per user decision)

    Resize browser window to 375px width -- verify content wraps properly.
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>Visual checkpoint approved -- all components render correctly with consistent dark theme, flat borders, and proper responsive behavior</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx tsc --noEmit` passes
- `cd apps/mobile && npx expo export --platform web` succeeds
- Human verification of all component variants in playground
</verification>

<success_criteria>
- Master barrel export enables `import { Button, Card, Typography } from '../../src/components'` pattern
- All 4 playground sections render in correct order: Core UI, Form Controls, Feedback, Layout
- Consistent dark theme, flat borders, no shadows throughout
- Mini form demo validates and shows errors/success
- Visual checkpoint approved by user
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-library/02-05-SUMMARY.md`
</output>
