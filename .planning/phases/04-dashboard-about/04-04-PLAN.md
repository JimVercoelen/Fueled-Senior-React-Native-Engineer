---
phase: 04-dashboard-about
plan: 04
type: execute
wave: 2
depends_on: [04-03]
files_modified:
  - apps/mobile/app/(dashboard)/about-me.tsx
  - apps/mobile/app/(dashboard)/about-app.tsx
  - apps/mobile/app/(dashboard)/about.tsx
  - apps/mobile/app/(dashboard)/_layout.tsx
autonomous: true
requirements: [ABUT-01, ABUT-02, ABUT-03, ABUT-04]
gap_closure: true

must_haves:
  truths:
    - "About Me screen shows author name, title, cover letter, tech tags, and contact links (LinkedIn/Portfolio/Email)"
    - "About App screen shows requirements checklist and tech stack rationale"
    - "Both screens are navigable from dashboard via their own routes"
    - "Both screens include ScrollToTop FAB for long content"
  artifacts:
    - path: "apps/mobile/app/(dashboard)/about-me.tsx"
      provides: "Personal intro screen with author info"
      min_lines: 40
    - path: "apps/mobile/app/(dashboard)/about-app.tsx"
      provides: "App info screen with requirements checklist and tech stack"
      min_lines: 50
    - path: "apps/mobile/app/(dashboard)/_layout.tsx"
      provides: "Updated routing with about-me and about-app screens"
      contains: "about-me"
  key_links:
    - from: "apps/mobile/app/(dashboard)/about-me.tsx"
      to: "apps/mobile/src/constants/author.ts"
      via: "import AUTHOR constant"
      pattern: "import.*AUTHOR"
    - from: "apps/mobile/app/(dashboard)/about-app.tsx"
      to: "apps/mobile/src/constants/requirements.ts"
      via: "import REQUIREMENTS constant"
      pattern: "import.*REQUIREMENTS"
    - from: "apps/mobile/app/(dashboard)/about-app.tsx"
      to: "apps/mobile/src/constants/tech-stack.ts"
      via: "import TECH_STACK constant"
      pattern: "import.*TECH_STACK"
---

<objective>
Split the monolithic About screen into two focused screens: "About Me" (personal intro) and "About App" (requirements + tech stack).

Purpose: UAT feedback requires separating personal and app content into distinct navigable screens, each with its own dashboard card.
Output: Two new screen files (about-me.tsx, about-app.tsx), updated layout routing, old about.tsx removed or redirected
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-about/04-UAT.md
@.planning/phases/04-dashboard-about/04-02-SUMMARY.md
@.planning/phases/04-dashboard-about/04-03-SUMMARY.md

@apps/mobile/app/(dashboard)/about.tsx
@apps/mobile/app/(dashboard)/_layout.tsx
@apps/mobile/src/constants/author.ts
@apps/mobile/src/constants/requirements.ts
@apps/mobile/src/constants/tech-stack.ts
@apps/mobile/src/components/index.ts

<interfaces>
<!-- Components available from @/components barrel -->
Typography, Avatar, Button, Accordion, AccordionItem, Badge, Card, Divider, ScrollToTop (created in plan 03)

<!-- Constants available (after plan 03 updates): -->
From src/constants/author.ts:
- AUTHOR: { name, title, coverLetter, techTags: string[], links: { label, url, icon }[] }
- title = "Senior Full Stack Engineer · 8+ years experience · 6+ years full-time remote for international companies"
- links = LinkedIn, Portfolio, Email

From src/constants/requirements.ts:
- REQUIREMENTS: RequirementGroup[] with { category, icon, requirements[] }

From src/constants/tech-stack.ts:
- TECH_STACK: TechStackItem[] with { name, category, rationale }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create About Me and About App screens</name>
  <files>apps/mobile/app/(dashboard)/about-me.tsx, apps/mobile/app/(dashboard)/about-app.tsx</files>
  <action>
**about-me.tsx** -- Personal introduction screen. Structure:

1. Import `{ useRef, useState }` from react, `{ View, ScrollView, Linking }` from react-native, `{ ScrollView as RNScrollView }` type for the ref, `MaterialIcons` from @expo/vector-icons, `{ Typography, Avatar, Button, Badge, ScrollToTop }` from @/components, `{ AUTHOR }` from @/constants/author.

2. ScrollView with `ref={scrollRef}` and `onScroll` handler that sets `showScrollTop` to true when `contentOffset.y > 300`. Set `scrollEventThrottle={16}`. Use same container pattern as existing about.tsx: `className="flex-1"`, `contentContainerClassName="p-4 pb-8 max-w-3xl mx-auto w-full gap-6"`, `style={{ backgroundColor: 'transparent' }}`.

3. **Page header section:**
   ```
   <Typography variant="h2" className="text-white">About Me</Typography>
   <Typography variant="body" className="text-white/60 mt-1">
     Get to know the engineer behind this showcase.
   </Typography>
   ```

4. **Author card** (same bg-white/5 border card style as current about.tsx):
   - Avatar (size="lg") + name + title in a flex-row
   - Cover letter as body text below
   - Tech tags rendered as a flex-row flex-wrap of Badge components (variant="default"): map over `AUTHOR.techTags`
   - Contact links as outlined buttons (same pattern as current about.tsx link buttons): map over `AUTHOR.links`, each Button with variant="outlined", icon cast as `keyof typeof MaterialIcons.glyphMap`, label, onPress calls Linking.openURL

5. **ScrollToTop** component at the bottom (outside ScrollView but inside the wrapping View): `<ScrollToTop scrollRef={scrollRef} visible={showScrollTop} />`

Wrap the entire screen in a `<View className="flex-1">` that contains the ScrollView and the ScrollToTop.

**about-app.tsx** -- App information screen. Structure:

1. Import same scroll/ref pattern. Import `{ Typography, Accordion, AccordionItem, ScrollToTop }` from @/components, `MaterialIcons`, `{ REQUIREMENTS }` from @/constants/requirements, `{ TECH_STACK }` from @/constants/tech-stack.

2. Same ScrollView container pattern with scrollRef and showScrollTop.

3. **Page header section:**
   ```
   <Typography variant="h2" className="text-white">About This App</Typography>
   <Typography variant="body" className="text-white/60 mt-1">
     A purpose-built technical showcase demonstrating production-quality React Native engineering.
   </Typography>
   ```

4. **Requirements checklist section** -- Copy the exact requirements checklist JSX from the current about.tsx (the Accordion with RequirementGroups, green checkmarks, IDs, titles, screen names). Keep it identical.

5. **Tech stack section** -- Copy the exact tech stack JSX from the current about.tsx (the cards with name, category, rationale). Keep it identical.

6. ScrollToTop at bottom, same pattern as about-me.

Both screens should follow the exact same visual patterns and NativeWind classes as the current about.tsx for consistency.
  </action>
  <verify>
    <automated>cd /Users/jimvercoelen/Desktop/Fueled-Senior-React-Native-Engineer/apps/mobile && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>about-me.tsx renders author info with Avatar, cover letter, tech tags, and LinkedIn/Portfolio/Email links. about-app.tsx renders requirements checklist accordion and tech stack cards. Both use ScrollToTop FAB.</done>
</task>

<task type="auto">
  <name>Task 2: Update layout routing and remove old about screen</name>
  <files>apps/mobile/app/(dashboard)/_layout.tsx, apps/mobile/app/(dashboard)/about.tsx</files>
  <action>
**_layout.tsx** -- Add the two new screen routes and remove the old `about` route:

1. In the `getScreenTitle` function's `titleMap`, remove `about: 'About'` and add:
   ```
   'about-me': 'About Me',
   'about-app': 'About App',
   ```

2. In the Stack children, remove `<Stack.Screen name="about" />` and add:
   ```
   <Stack.Screen name="about-me" />
   <Stack.Screen name="about-app" />
   ```

**about.tsx** -- Delete this file entirely. It is replaced by about-me.tsx and about-app.tsx. Use the Bash tool to `rm` the file.
  </action>
  <verify>
    <automated>cd /Users/jimvercoelen/Desktop/Fueled-Senior-React-Native-Engineer/apps/mobile && ls app/\(dashboard\)/about-me.tsx app/\(dashboard\)/about-app.tsx && ! ls app/\(dashboard\)/about.tsx 2>/dev/null && echo "PASS: Routes correct"</automated>
  </verify>
  <done>_layout.tsx routes about-me and about-app screens. Old about.tsx is deleted. Title map shows "About Me" and "About App" in the header breadcrumb.</done>
</task>

</tasks>

<verification>
- `ls apps/mobile/app/(dashboard)/about-me.tsx` exists
- `ls apps/mobile/app/(dashboard)/about-app.tsx` exists
- `ls apps/mobile/app/(dashboard)/about.tsx` does NOT exist
- `grep "about-me" apps/mobile/app/(dashboard)/_layout.tsx` returns a match
- `grep "about-app" apps/mobile/app/(dashboard)/_layout.tsx` returns a match
</verification>

<success_criteria>
- About Me screen shows author name, title, cover letter, tech tags as badges, and 3 contact link buttons (LinkedIn, Portfolio, Email)
- About App screen shows requirements checklist in accordion and tech stack cards with rationale
- Both screens have ScrollToTop FAB
- Old about.tsx deleted, _layout.tsx updated with new routes
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-about/04-04-SUMMARY.md`
</output>
