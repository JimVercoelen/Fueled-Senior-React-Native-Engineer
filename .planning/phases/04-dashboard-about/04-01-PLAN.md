---
phase: 04-dashboard-about
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(dashboard)/index.tsx
  - apps/mobile/tests/screens/dashboard.test.tsx
  - apps/mobile/tests/setup.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03]

must_haves:
  truths:
    - "Dashboard shows a welcome greeting with the logged-in user's email above the cards"
    - "Dashboard still renders 4 rich cards that navigate to demo screens (DASH-01 already done)"
    - "Persistent header with logo and profile icon remains visible (DASH-03 already done)"
  artifacts:
    - path: "apps/mobile/app/(dashboard)/index.tsx"
      provides: "Welcome greeting section using useSession"
      contains: "useSession"
    - path: "apps/mobile/tests/screens/dashboard.test.tsx"
      provides: "Unit tests for DASH-01, DASH-02, DASH-03"
      min_lines: 30
  key_links:
    - from: "apps/mobile/app/(dashboard)/index.tsx"
      to: "src/contexts/auth.tsx"
      via: "useSession() hook for email"
      pattern: "useSession.*session.*email"
---

<objective>
Add a personalized welcome greeting to the dashboard screen showing the authenticated user's email, and create unit tests covering all three DASH requirements.

Purpose: DASH-02 is the only remaining dashboard requirement. DASH-01 (4 rich cards) and DASH-03 (persistent header) were completed during Phase 3's UX checkpoint. This plan adds the greeting and validates all three requirements with tests.
Output: Updated dashboard screen with greeting, dashboard test file with passing tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/mobile/app/(dashboard)/index.tsx
@apps/mobile/app/(dashboard)/_layout.tsx
@apps/mobile/src/contexts/auth.tsx
@apps/mobile/tests/contexts/auth.test.tsx
@apps/mobile/tests/setup.ts
@apps/mobile/tests/__mocks__/supabase.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/contexts/auth.tsx:
```typescript
interface AuthContextValue {
  session: Session | null;
  isLoading: boolean;
  signIn: (email: string) => Promise<{ error: AuthError | null }>;
  signOut: () => Promise<void>;
}
export function useSession(): AuthContextValue;
```

From tests/__mocks__/supabase.ts:
```typescript
export const mockSupabase = { auth: { getSession, signInWithOtp, signOut, onAuthStateChange } };
export function createMockSession(overrides?: Partial<Session>): Session;
// Default mock session has email: 'test@example.com'
```

From tests/setup.ts (existing mocks):
```typescript
// Already mocked: react-native-reanimated, expo-linear-gradient, @expo/vector-icons
// Need to ADD: expo-router mock (useRouter, usePathname)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add welcome greeting to dashboard</name>
  <files>apps/mobile/app/(dashboard)/index.tsx</files>
  <action>
Add `useSession` import from `@/contexts/auth` to the dashboard screen. Insert a welcome greeting section above the DEMO_CARDS.map block inside the existing ScrollView. The greeting shows:
- A `Typography variant="h2"` with text "Welcome back" in white
- Below it, if `session?.user?.email` exists, a `Typography variant="body"` with `className="text-white/60 mt-1"` showing the email

The greeting should be wrapped in a `View` with `className="mb-2"`.

Import `useSession` from `@/contexts/auth`. Extract `session` from `useSession()` at the top of the `DashboardScreen` component. Use `session?.user?.email` with optional chaining -- do NOT destructure email directly since session can be null.

Do NOT modify the existing DashboardCard component or DEMO_CARDS array. Only add the greeting to the DashboardScreen component's return.
  </action>
  <verify>
    <automated>cd /Users/jimvercoelen/Desktop/Fueled-Senior-React-Native-Engineer/apps/mobile && npx jest tests/screens/dashboard.test.tsx --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>Dashboard screen imports useSession, displays "Welcome back" heading and user email from session above the 4 cards. Null session handled gracefully with optional chaining.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create dashboard screen tests</name>
  <files>apps/mobile/tests/screens/dashboard.test.tsx, apps/mobile/tests/setup.ts</files>
  <behavior>
    - DASH-02: Renders "Welcome back" text when session exists
    - DASH-02: Renders user email "test@example.com" from mock session
    - DASH-02: Does NOT render email when session is null
    - DASH-01: Renders all 4 card titles ("Data Fetching", "State Management", "Component Library", "About")
    - DASH-03: Header renders logo and profile icon (tested via layout mock or noted as integration-level)
  </behavior>
  <action>
First, add mocks to `tests/setup.ts` for `expo-router` (useRouter returns `{ push: jest.fn() }`, usePathname returns `'/'`). These are needed because the dashboard imports from expo-router.

Then replace the stub `tests/screens/dashboard.test.tsx` with real tests:

1. Mock `../../src/lib/supabase` to use `mockSupabase` (same pattern as auth.test.tsx)
2. Mock `expo-router` with `useRouter` returning `{ push: jest.fn() }` and `usePathname` returning `'/'`
3. Mock `react-native/Libraries/Linking/Linking` with `openURL: jest.fn()`
4. Import `AuthProvider` from `../../src/contexts/auth`
5. Import `createMockSession` from `../__mocks__/supabase`
6. Import `DashboardScreen` from the screen file (default export)

Create a `renderDashboard` helper that wraps DashboardScreen in AuthProvider.

Tests:
- **"renders welcome greeting with user email"**: Set mockSupabase.auth.getSession to return a session with email "test@example.com". Render. waitFor "Welcome back" text to appear. Assert "test@example.com" is also rendered.
- **"hides email when session is null"**: Default mock (null session). Render. waitFor loading to settle. Assert "Welcome back" is still rendered. Assert "test@example.com" is NOT in the document.
- **"renders all 4 demo card titles"**: Render. Assert "Data Fetching", "State Management", "Component Library", "About" are all present (these come from DEMO_CARDS constant).
- **"renders Explore text on each card"**: Render. Assert 4 instances of "Explore" text.

Run `cd apps/mobile && npx jest tests/screens/dashboard.test.tsx --no-coverage` to verify all pass.
  </action>
  <verify>
    <automated>cd /Users/jimvercoelen/Desktop/Fueled-Senior-React-Native-Engineer/apps/mobile && npx jest tests/screens/dashboard.test.tsx --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>All dashboard tests pass. DASH-01 validated (4 cards render), DASH-02 validated (greeting with email), DASH-03 noted as layout-level (header exists in _layout.tsx, tested via integration).</done>
</task>

</tasks>

<verification>
cd /Users/jimvercoelen/Desktop/Fueled-Senior-React-Native-Engineer/apps/mobile && npx jest tests/screens/dashboard.test.tsx --no-coverage
</verification>

<success_criteria>
- Dashboard screen shows "Welcome back" and authenticated user email above the 4 cards
- All dashboard tests pass (greeting with email, greeting without email, 4 card titles render)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-about/04-01-SUMMARY.md`
</output>
