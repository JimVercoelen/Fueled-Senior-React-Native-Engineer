---
phase: 04-dashboard-about
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified:
  - apps/mobile/app/(dashboard)/index.tsx
  - apps/mobile/tests/screens/dashboard.test.tsx
  - apps/mobile/tests/screens/about-me.test.tsx
  - apps/mobile/tests/screens/about-app.test.tsx
  - apps/mobile/tests/screens/about.test.tsx
autonomous: true
requirements: [DASH-01, ABUT-01, ABUT-02, ABUT-03, ABUT-04]
gap_closure: true

must_haves:
  truths:
    - "Dashboard shows 5 numbered cards with About Me first and About App second"
    - "Cards display numbered indicators (1-5) showing viewing order"
    - "All card routes are correct (about-me, about-app, data-fetching, state-management, components)"
    - "Tests validate new card structure, About Me content, and About App content"
  artifacts:
    - path: "apps/mobile/app/(dashboard)/index.tsx"
      provides: "Dashboard with 5 numbered cards, About Me first"
      contains: "About Me"
    - path: "apps/mobile/tests/screens/about-me.test.tsx"
      provides: "Tests for About Me screen"
      min_lines: 30
    - path: "apps/mobile/tests/screens/about-app.test.tsx"
      provides: "Tests for About App screen"
      min_lines: 30
    - path: "apps/mobile/tests/screens/dashboard.test.tsx"
      provides: "Updated dashboard tests for 5 cards"
      contains: "About Me"
  key_links:
    - from: "apps/mobile/app/(dashboard)/index.tsx"
      to: "apps/mobile/app/(dashboard)/about-me.tsx"
      via: "router.push route"
      pattern: "about-me"
    - from: "apps/mobile/app/(dashboard)/index.tsx"
      to: "apps/mobile/app/(dashboard)/about-app.tsx"
      via: "router.push route"
      pattern: "about-app"
    - from: "apps/mobile/tests/screens/about-me.test.tsx"
      to: "apps/mobile/app/(dashboard)/about-me.tsx"
      via: "import and render"
      pattern: "import.*about-me"
---

<objective>
Reorder dashboard cards (About Me first, numbered 1-5) and update all tests to match the new 2-screen About structure.

Purpose: UAT feedback requires About to introduce the app first (card 1), followed by About App (card 2), then existing demo cards. Tests must be updated for the new screen split.
Output: Updated dashboard with 5 numbered cards, new test files for both About screens, updated dashboard tests
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-about/04-UAT.md
@.planning/phases/04-dashboard-about/04-02-SUMMARY.md
@.planning/phases/04-dashboard-about/04-03-SUMMARY.md
@.planning/phases/04-dashboard-about/04-04-SUMMARY.md

@apps/mobile/app/(dashboard)/index.tsx
@apps/mobile/tests/screens/dashboard.test.tsx
@apps/mobile/tests/screens/about.test.tsx

<interfaces>
<!-- Dashboard index.tsx current structure -->
DemoCard type: { title, subtitle, tags, route, image }
DEMO_CARDS array: 4 cards (Data Fetching, State Management, Component Library, About)
DashboardCard component: renders each card with animations
DashboardScreen: renders welcome greeting + card list

<!-- After plan 03, author.ts exports: -->
AUTHOR: { name, title, coverLetter, techTags, links[] }

<!-- After plan 04, new screen files: -->
apps/mobile/app/(dashboard)/about-me.tsx - imports AUTHOR from @/constants/author
apps/mobile/app/(dashboard)/about-app.tsx - imports REQUIREMENTS from @/constants/requirements, TECH_STACK from @/constants/tech-stack

<!-- Card images available: -->
about.jpg, component-library.jpg, data-fetching.jpg, state-management.jpg
(about.jpg will be reused for both About Me and About App cards)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder and expand dashboard cards to 5 with numbering</name>
  <files>apps/mobile/app/(dashboard)/index.tsx</files>
  <action>
Update the `DEMO_CARDS` array in `index.tsx` to have 5 cards in this exact order:

```typescript
const DEMO_CARDS: DemoCard[] = [
  {
    title: 'About Me',
    subtitle: 'Meet the engineer behind this showcase. 8+ years of full stack experience across TypeScript, React, Node.js, and Python.',
    tags: ['Cover Letter', 'Contact', 'Portfolio'],
    route: '/(dashboard)/about-me',
    image: require('../../assets/images/cards/about.jpg'),
  },
  {
    title: 'About This App',
    subtitle: 'Fueled requirements checklist and tech stack rationale. See what was built and why.',
    tags: ['Requirements', 'Tech Stack', 'Checklist'],
    route: '/(dashboard)/about-app',
    image: require('../../assets/images/cards/about.jpg'),
  },
  {
    title: 'Data Fetching',
    subtitle: 'TanStack Query, pagination, search, filter, and mutations with real API integration.',
    tags: ['TanStack Query', 'API', 'Pagination'],
    route: '/(dashboard)/data-fetching',
    image: require('../../assets/images/cards/data-fetching.jpg'),
  },
  {
    title: 'State Management',
    subtitle: 'Cache viewer, toast notifications, and modal system powered by Context API.',
    tags: ['Context API', 'Cache', 'Toasts'],
    route: '/(dashboard)/state-management',
    image: require('../../assets/images/cards/state-management.jpg'),
  },
  {
    title: 'Component Library',
    subtitle: 'Interactive playground showcasing all UI components with live demos.',
    tags: ['NativeWind', 'Reanimated', 'Forms'],
    route: '/(dashboard)/components',
    image: require('../../assets/images/cards/component-library.jpg'),
  },
];
```

Add a **number indicator** to each card. In the `DashboardCard` component, accept an `index` prop (number). Modify the `DashboardCard` component:

1. Update the component signature: `function DashboardCard({ card, index }: { card: DemoCard; index: number })`

2. Inside the `LinearGradient` content area, add a numbered badge at the top-left before the title:
```tsx
<View className="flex-row items-center mb-3">
  <View
    style={{
      width: 28,
      height: 28,
      borderRadius: 14,
      backgroundColor: 'rgba(255,255,255,0.2)',
      alignItems: 'center',
      justifyContent: 'center',
    }}
  >
    <Typography variant="caption" className="text-white font-semibold">
      {index + 1}
    </Typography>
  </View>
</View>
```

3. Update the map call to pass index:
```tsx
{DEMO_CARDS.map((card, index) => (
  <DashboardCard key={card.route} card={card} index={index} />
))}
```

Also add the ScrollToTop FAB to the dashboard. Import `{ useRef, useState }` (useState already imported), `ScrollToTop` from @/components, and `ScrollView as RNScrollView` type. Add scrollRef and showScrollTop state. Wrap the ScrollView with a `<View className="flex-1">` container. Add ref and onScroll to ScrollView. Place `<ScrollToTop scrollRef={scrollRef} visible={showScrollTop} />` after the ScrollView inside the View wrapper.
  </action>
  <verify>
    <automated>cd /Users/jimvercoelen/Desktop/Fueled-Senior-React-Native-Engineer/apps/mobile && grep -c "About Me\|About This App\|Data Fetching\|State Management\|Component Library" app/\(dashboard\)/index.tsx</automated>
  </verify>
  <done>Dashboard shows 5 cards in order: About Me (1), About This App (2), Data Fetching (3), State Management (4), Component Library (5). Each card has a numbered circle indicator. ScrollToTop FAB present.</done>
</task>

<task type="auto">
  <name>Task 2: Update and create test files for new screen structure</name>
  <files>apps/mobile/tests/screens/dashboard.test.tsx, apps/mobile/tests/screens/about-me.test.tsx, apps/mobile/tests/screens/about-app.test.tsx, apps/mobile/tests/screens/about.test.tsx</files>
  <action>
**Delete** `apps/mobile/tests/screens/about.test.tsx` -- the old About screen no longer exists. Use `rm` via Bash.

**Update dashboard.test.tsx:**
- Change the test "renders all 4 demo card titles" to "renders all 5 demo card titles"
- Update assertions to check for: 'About Me', 'About This App', 'Data Fetching', 'State Management', 'Component Library'
- Remove the assertion for just 'About' (old card)
- Update the "Explore" text count assertion from 4 to 5
- Keep all other tests unchanged (welcome greeting, email display)

**Create about-me.test.tsx:**
```typescript
import React from 'react';
import { render, fireEvent } from '@testing-library/react-native';
import { Linking } from 'react-native';
import AboutMeScreen from '../../app/(dashboard)/about-me';

const openURLSpy = jest.spyOn(Linking, 'openURL').mockResolvedValue(true as never);

function renderAboutMe() {
  return render(<AboutMeScreen />);
}

beforeEach(() => {
  jest.clearAllMocks();
});

describe('AboutMeScreen', () => {
  // ABUT-02: Author section
  describe('ABUT-02: Author section', () => {
    it('renders author name and title', () => {
      const { getByText } = renderAboutMe();
      expect(getByText('Jim Vercoelen')).toBeTruthy();
      expect(getByText(/Senior Full Stack Engineer/)).toBeTruthy();
    });

    it('renders updated cover letter content', () => {
      const { getByText } = renderAboutMe();
      expect(getByText(/senior full stack engineer with over 8 years/)).toBeTruthy();
    });

    it('renders tech tags', () => {
      const { getByText } = renderAboutMe();
      expect(getByText('TypeScript')).toBeTruthy();
      expect(getByText('React Native')).toBeTruthy();
      expect(getByText('Python')).toBeTruthy();
    });
  });

  // ABUT-04: External links (LinkedIn, Portfolio, Email)
  describe('ABUT-04: External links', () => {
    it('renders LinkedIn, Portfolio, and Email buttons', () => {
      const { getByText } = renderAboutMe();
      expect(getByText('LinkedIn')).toBeTruthy();
      expect(getByText('Portfolio')).toBeTruthy();
      expect(getByText('Email')).toBeTruthy();
    });

    it('calls Linking.openURL when LinkedIn pressed', () => {
      const { getByText } = renderAboutMe();
      fireEvent.press(getByText('LinkedIn'));
      expect(Linking.openURL).toHaveBeenCalledWith('https://linkedin.com/in/jimvercoelen');
    });

    it('calls Linking.openURL with mailto for email', () => {
      const { getByText } = renderAboutMe();
      fireEvent.press(getByText('Email'));
      expect(Linking.openURL).toHaveBeenCalledWith('mailto:jim@vecotech.io');
    });
  });
});
```

**Create about-app.test.tsx:**
```typescript
import React from 'react';
import { render } from '@testing-library/react-native';
import AboutAppScreen from '../../app/(dashboard)/about-app';

function renderAboutApp() {
  return render(<AboutAppScreen />);
}

describe('AboutAppScreen', () => {
  // ABUT-01: Requirements checklist
  describe('ABUT-01: Requirements checklist', () => {
    it('renders requirement category headings', () => {
      const { getByText, getAllByText } = renderAboutApp();
      expect(getByText('Authentication')).toBeTruthy();
      expect(getAllByText('Dashboard').length).toBeGreaterThanOrEqual(1);
      expect(getByText('Component Library')).toBeTruthy();
      expect(getByText('Data Fetching')).toBeTruthy();
      expect(getByText('State Management')).toBeTruthy();
    });

    it('renders requirement IDs', () => {
      const { getByText } = renderAboutApp();
      expect(getByText('AUTH-01')).toBeTruthy();
      expect(getByText('DASH-01')).toBeTruthy();
      expect(getByText('COMP-01')).toBeTruthy();
    });
  });

  // ABUT-03: Tech stack
  describe('ABUT-03: Tech stack', () => {
    it('renders tech stack items', () => {
      const { getByText } = renderAboutApp();
      expect(getByText('React Native (Expo SDK 55)')).toBeTruthy();
      expect(getByText('TypeScript')).toBeTruthy();
      expect(getByText('NativeWind v4')).toBeTruthy();
    });

    it('renders tech stack rationale', () => {
      const { getByText } = renderAboutApp();
      expect(getByText(/Cross-platform with web deployment/)).toBeTruthy();
      expect(getByText(/Compile-time safety/)).toBeTruthy();
    });
  });
});
```

Note: The test files follow the exact same patterns established in phase 04-02 (jest.spyOn for Linking, render helper function, describe blocks per requirement ID). If any test fails due to the ScrollToTop component using Reanimated (which is already mocked globally in setup.ts), no additional mocking should be needed since the inline reanimated mock from 04-01 handles useSharedValue/useAnimatedStyle/withTiming.
  </action>
  <verify>
    <automated>cd /Users/jimvercoelen/Desktop/Fueled-Senior-React-Native-Engineer/apps/mobile && npx jest tests/screens/ --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <done>Dashboard tests pass with 5 card assertions. about-me.test.tsx passes with author name, title, cover letter, tech tags, and LinkedIn/Email link tests. about-app.test.tsx passes with requirements checklist and tech stack tests. Old about.test.tsx deleted. Full test suite green.</done>
</task>

</tasks>

<verification>
- `cd apps/mobile && npx jest tests/screens/ --no-coverage` -- all screen tests pass
- `cd apps/mobile && npx jest --no-coverage` -- full test suite passes with no regressions
- Dashboard shows 5 cards: About Me, About This App, Data Fetching, State Management, Component Library
- Each card has a numbered circle (1-5)
- Old about.test.tsx does not exist
</verification>

<success_criteria>
- Dashboard displays 5 numbered cards with About Me as card 1 and About App as card 2
- All 3 test files (dashboard, about-me, about-app) pass
- Old about.test.tsx removed
- Full test suite has zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-about/04-05-SUMMARY.md`
</output>
